# BEGIN MANAGED: configure-sol-luna-subagents:model-tiers
# profile: astra-luna
# Tiers select provider/model only; role permissions stay in agent TOML files.

[tiers.T1]
enabled = true
model_provider = "openai"
model = "gpt-6-astra"
supported_efforts = ["low", "medium", "high", "max"]

[tiers.T2]
enabled = true
model_provider = "openai"
model = "gpt-5.6-luna"
supported_efforts = ["low", "medium", "high", "xhigh", "max"]

[tiers.T3]
enabled = true
model_provider = "openai"
model = "gpt-5.6-luna"
supported_efforts = ["low", "medium", "high", "xhigh", "max"]
# END MANAGED: configure-sol-luna-subagents:model-tiers
