interface:
  display_name: "Configure Astra-Luna Subagents"
  short_description: "Preview and safely configure a global Astra-to-Luna baseline"
  default_prompt: "Use $configure-sol-luna-subagents to preview the global Astra-to-Luna baseline, then apply it only after model availability is confirmed."
policy:
  allow_implicit_invocation: false
